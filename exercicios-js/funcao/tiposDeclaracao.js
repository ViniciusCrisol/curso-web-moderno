// function ddeclaration
function soma(x, y) {
  return x + y;
}

// function expression
const sub = (x, y) => {
  return x - y;
};

// amed function expression
const mult = function mult(x, y) {
  return x * y;
};
